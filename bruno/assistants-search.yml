info:
  name: assistants/search
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:8123/assistants/search
  body:
    type: json
    data: "{}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
