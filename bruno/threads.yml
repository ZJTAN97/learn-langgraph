info:
  name: threads
  type: http
  seq: 2

http:
  method: POST
  url: http://localhost:8123/threads
  body:
    type: json
    data: "{}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
